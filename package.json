{
  "name": "empty_typescript_project",
  "version": "1.0.0",
  "description": "Boilerplate TypeScript project.",
  "main": "./build/main/index.js",
  "scripts": {
    "B_S": "tsc --build --pretty ./tsconfig.json; node ./build/main;",
    "build": "tsc --build --pretty ./tsconfig.json",
    "start": "node ./build/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "PatMan10",
  "license": "GPL-3.0-or-later"
}
